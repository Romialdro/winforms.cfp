using System;

public static class DbConfig
{
    // Cadena de conexión sin password (root por defecto en XAMPP)
    public static string ConnectionString = "server=localhost;user=root;database=club_app;";
}